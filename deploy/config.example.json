{
  "listen": "0.0.0.0:8443",

  "tls": {
    "cert_file": "/etc/gripline/tls/cert.pem",
    "key_file": "/etc/gripline/tls/key.pem",
    "min_version": "1.3"
  },

  "backend": {
    "url": "https://provider.internal:443/v1",
    "timeout": "30s",
    "dial_timeout": "5s",
    "tls_handshake_timeout": "5s",
    "response_header_timeout": "20s",
    "max_idle_conns_per_host": 64
  },

  "server": {
    "read_timeout": "30s",
    "write_timeout": "60s",
    "idle_timeout": "120s",
    "read_header_timeout": "10s",
    "max_header_bytes": 65536,
    "max_body_bytes": 33554432,
    "stream_write_idle_timeout": "30s"
  },

  "admin": {
    "listen": "127.0.0.1:9090",
    "operator_tokens": {
      "${GRIPLINE_OPERATOR_TOKEN}": "ops:posture.control,credential.lifecycle,lane.lifecycle"
    }
  },

  "identity": {
    "audience": "fi-inference"
  },

  "ingress": {
    "pseudonym_key": "${GRIPLINE_PSEUDONYM_KEY}",
    "trusted_proxies": ["10.0.0.0/8"]
  },

  "paths": {
    "state": "/var/lib/gripline/state.db",
    "signer_keyring": "/var/lib/gripline/keyring.json",
    "audit_log": "/var/lib/gripline/audit.jsonl"
  },

  "deployment": {}
}
